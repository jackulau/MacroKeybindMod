# build
build/
.gradle/
.kotlin/
*.class
out/
# IDE
.idea/
*.iml
.vscode/
# goal/workspace state (per-developer)
.claude-workspace/
# reference material — local study only, never tracked or published
reference/
tools/*.jar
run/
.stonecutter/
site/
.claude/
# OS cruft
.DS_Store

# NOTE: dist/ is intentionally TRACKED — it holds the ready-to-install release jars.
